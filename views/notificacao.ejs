<!DOCTYPE html>

<html lang="pt-br">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Registre notificações e denúncias sobre acessibilidade urbana no Sistema de Apoio e Acessibilidade (S.A.A.).">
        <title>Notificação</title>
        <link rel="stylesheet" href="/css/notificacao.css">
        <link rel="stylesheet" href="/css/botoes-voltar-e-avancar.css">
    </head>
    
    <body>
        <header>

        </header>

        <main>
            <div class="container">
                <h1>Notificação</h1>
                <section>
                    <div class="card">
                        <form action="<%= values && values._id ? '/notificacao/editar/' + values._id : '/notificacao' %>" method="post" enctype="multipart/form-data">
                            <%- include('partials/cadastrar-local') %>
                        </form>
                    </div>
                </section>
            </div>
        </main>

        <footer>
            
        </footer>

        <script src="js/botoes-voltar-e-avancar.js"></script>
        <script src="js/cadastrar-imagem.js"></script>
        <script src="js/estrela.js"></script>

    </body>

</html>